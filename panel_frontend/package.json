{
  "name": "woow-esphome-modbus-scanner-panel",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.js",
    "build": "rollup -c",
    "deploy": "npm run build && node scripts/deploy.mjs",
    "check:drift": "npm run build && node scripts/check-drift.mjs",
    "test:browser": "node ../tests/panel/browser_check.mjs"
  },
  "dependencies": {
    "lit": "3.3.1"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "16.0.1",
    "playwright": "1.62.1",
    "rollup": "4.63.1"
  }
}
